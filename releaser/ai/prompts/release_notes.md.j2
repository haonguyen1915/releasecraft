Generate concise, high-impact release notes for version **{{ current_version }}** (from {{ previous_version }}).

## Commits ({{ commits|length }} total)
{% for commit in commits %}
- **{{ commit.hash[:8] }}**: {{ commit.message }}
{% endfor %}

{% if diffs %}

## Code Changes (for context)

The following diffs provide additional context for important commits:

{% for commit_hash, diff_text in diffs.items() %}
**{{ commit_hash[:8] }}**:
```diff
{{ diff_text[:2000] }}{% if diff_text|length > 2000 %} ... (truncated){% endif %}
```
{% endfor %}
{% endif %}

---

## Your Task

Create release notes with **3-5 impactful highlights** that capture what truly matters in this release.

**Remember**:
- Commits already use conventional types (feat:, fix:, etc.) - don't repeat that categorization
- Focus on HIGH-LEVEL impact, not granular details
- Combine related commits into themes
- Skip minor changes (chores, docs, tests unless significant)
- Be concise and action-oriented

**Output**:
1. **summary** (optional): One sentence describing the release theme
2. **highlights**: 3-5 punchy bullet points of the most important changes
3. **breaking_changes**: Any breaking changes (look for "!" or "BREAKING CHANGE")

Quality over quantity!